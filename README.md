# ground-backend

access using `hostname/api/`

## GET routes:

- /
- /user/`[username]`

## POST routes:

- /register
  - username, password
- /login
  - username, password
- /home
  - token
- /new
  - token, message
- /follow
  - token, followingName
- /unfollow
  - token, followingName
